@{
	ViewData["Title"] = "Subjects";
}
<div class="background-box">
	<div class="text-center">
		<div id="title-inline"></div>
		<h2>Предметы</h2>
		<div id="title-inline"></div>

		<div>
			<p>
				<form asp-controller="Subjects" asp-action="All">
					<button>Предметы</button>
				</form>
			</p>

			<p>
				<form asp-controller="Subjects" asp-action="Add">
					<button>Предметы</button>
				</form>
			</p>
		</div>

		<div>
			<p>Поиск по номеру записи:</p>
			<form asp-controller="Subjects" asp-action="Result" method="GET">
				@Html.TextBox("id", "1", new { type="number", required = "required", min="1", placeholder="1"})
				<button>Найти</button>
			</form>
			<a asp-controller="Subjects" asp-action="Search">Расширеный поиск</a>
		</div>
	</div>
</div>
